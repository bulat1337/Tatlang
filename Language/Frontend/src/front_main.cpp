#include <stdio.h>

#include "frontend.h"
#include "b_tree.h"

int main()
{
// 	B_tree_node *node = create_node(OP, {.op_value = ADD}, NULL, NULL).arg.node;
//
// 	GR_DUMP_CODE_GEN(node);

	return 0;
}
